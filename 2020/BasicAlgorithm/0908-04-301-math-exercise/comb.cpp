#include <iostream>
using namespace std;

void comb(int ** ans, int arr[], int i, int f, int n)
{
    if (n = 0) {
        //
    }
}

int main(void)
{
    int arr[] = {1, 2, 3, 4, 5};
    int size = sizeof(arr) / sizeof(arr[0]);
    int ** ans;
    comb(ans, arr, 0, size - 1, 2);
    return 0;
}